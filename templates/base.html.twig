
        <!-- Inclus le composant header -->
        {% include 'components/header.html.twig' %}
        {% block body %}
        <div class="content">
                <!-- Composant 1 : Formulaire -->
                <div id="composant1" class="{#composant {% if displayForm %} visible {% else %} hidden {% endif %}#}">
                        {#% include 'components/ingredientForm.html.twig' %#}
                </div>

                <!-- Composant 2 : Liste -->
                <div id="composant2" class="{#composant {% if displayList %} visible {% else %} hidden {% endif %}#}">
                        {#% include 'components/listRecettes.html.twig' %#}
                </div>

                <!-- Composant 3 : Détails -->
                <div id="composant3" class="{#composant {% if displayDetails %} visible {% else %} hidden {% endif %}#}">
                        {% include 'components/detailRecette.html.twig' %}
                </div>
        </div>

        {% endblock %}
        <!-- Inclus le composant footer -->
        {% include 'components/footer.html.twig' %}
